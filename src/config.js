export const port = process.env.PORT || 9200;
export const host = process.env.WEBSITE_HOSTNAME || `localhost: ${port}`;

/* Facebook, Twitter, Google... */
export const auth = {

};

/* Firebase, Google, Mixpanel... */
export const analytics = {
};
